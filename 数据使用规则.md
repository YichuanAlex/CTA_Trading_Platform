如果需要使用5m的数据，可以用1m合成5m，主力合约需要你找出来，持仓量open_interest最大的就是主力合约
通过api.get_daily_symbol()可以获取每个交易日的期货合约，其中的expire_date是到期日，主力合约在距离到期日前60天进行强制切换

df_daily_symbol = api.get_daily_symbol()
if df daily symbol is not None:
    for tup in df daily symbol.itertuples():
        symbol = getattr(tup,"symbol")
        product id = getattr(tup,"product id")
        self.dict product id[symbol]= product id
        self.dict expire date[symbol]= getattr(tup, "expire_date")
    pass